import { StyleSheet } from "react-native";

export default StyleSheet.create({
  header: {
    marginBottom: 10,
    alignItems: "center",
  },
  headerText: {
    fontSize: 17,
    opacity: 0.5,
  },
  headerTextBold: {
    fontWeight: "bold",
  },
});
